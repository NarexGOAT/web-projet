<!DOCTYPE html>
<html lang="fr-FR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ma Wish-list - BHStage</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/wishlist.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body>
<!-- OVERLAY -->
<div id="menu-overlay" class="menu-overlay"></div>

<!-- MENU BURGER -->
<div id="menu-burger" class="menu-burger">

    <button id="close-menu" class="close-btn">✕</button>

    <div class="menu-content">

        <a href="index.php">🏠 Accueil</a>
        <a href="index.php?page=offres">💼 Offres de stages</a>
        <a href="index.php?page=entreprises">🏢 Entreprises</a>

        {% if session.user is defined %}

            {% if session.user.role == 'etudiant' %}
                <a href="index.php?page=wishlist">❤️ Ma Wish-list</a>
                <a href="index.php?page=mes-candidatures">📄 Mes candidatures</a>

            {% elseif session.user.role == 'recruteur' %}
                <a href="index.php?page=gestion-etudiants">🎓 Gestion des étudiants</a>

            {% elseif session.user.role == 'admin' %}
                <a href="index.php?page=gestion-pilotes">👨‍✈️ Gestion des pilotes</a>
                <a href="index.php?page=gestion-etudiants">🎓 Gestion des étudiants</a>

            {% endif %}

        {% endif %}

    </div>

    <a href="index.php?page=logout" class="btn-logout">
        🔴 Déconnexion
    </a>

</div>

<!-- HEADER -->
<header class="entete">
    <div class="logo">
        <a href="index.php">
            <img src="/images/allostage.png" alt="Accueil AlloStage" class="logo-img">
        </a>
    </div>

    <nav class="navigation">
        <ul>
            <li><a href="index.php">Accueil</a></li>
            <li><a href="index.php?page=offres">Offres de stages</a></li>
            <li><a href="index.php?page=entreprises">Entreprises</a></li>

            {% if session.user is defined and session.user.role == 'etudiant' %}
                <li><a href="index.php?page=wishlist" class="actif">Ma Wish-list</a></li>
                <li><a href="index.php?page=mes-candidatures">Mes candidatures</a></li>
            {% endif %}
        </ul>
    </nav>

    <div class="actions-entete">
        {% if session.user is defined %}
            <button id="open-menu" class="btn-principal">
                Espace {{ session.user.role_label ?? session.user.role|capitalize }} ▾
            </button>
        {% else %}
            <a href="index.php?page=connexion" class="btn-connexion">
                Se connecter
            </a>
        {% endif %}
    </div>
</header>

    <main class="contenu fond-gris">
        
        <section class="en-tete-recherche">
            <h1>Ma Wish-list ❤️</h1>
            <p>Retrouvez ici toutes les offres de stage que vous avez sauvegardées pour y postuler plus tard.</p>
        </section>

        <section class="conteneur-liste">
            
            {% if offres is empty %}
                <p>Vous n'avez pas encore d'offres dans votre wish-list.</p>
            {% else %}
                <div class="grille-offres">
                    {% for offre in offres %}
                        <article class="carte-offre">
                            <div class="en-tete-carte">
                                <span class="badge-vide"></span>
                                <a class="btn-favori actif" 
                                   title="Retirer de la wish-list"
                                   href="index.php?page=wishlist-supprimer&id={{ offre.id_offre }}">
                                    <i class="fas fa-heart"></i>
                                </a>
                            </div>

                            <h3 class="titre-offre">{{ offre.titre }}</h3>
                            <p class="entreprise-offre">
                                <strong>{{ offre.nom_entreprise ?? 'Entreprise' }}</strong>
                                {% if offre.ville %}
                                  • {{ offre.ville }}
                                {% endif %}
                            </p>

                            <div class="tags-offre">
                                <span class="tag">📅 {{ offre.duree_stage }} semaines</span>
                                {% if offre.competence %}
                                    <span class="tag">🛠 {{ offre.competence }}</span>
                                {% endif %}
                            </div>

                            <p class="description">{{ offre.description }}</p>

                            <div class="actions-carte">
                                <a href="index.php?page=offre&id={{ offre.id_offre }}" class="btn-secondaire">Détails</a>

                            {% if offre.id_offre in idsCandidatures %}
                                <a href="index.php?page=candidatures" class="btn-postuler-lien">
                                    Voir mes candidatures
                                </a>
                            {% else %}
                                <a href="index.php?page=postuler&id={{ offre.id_offre }}" class="btn-postuler-lien">
                                    Postuler
                                </a>
                            {% endif %}
                            </div>
                        </article>
                    {% endfor %}
                </div>
            {% endif %}

        </section>
    </main>

    <footer class="pied-de-page">
        <p>&copy; 2026 BHStage. Tous droits réservés.</p>
    </footer>
    <script>
const openBtn  = document.getElementById('open-menu');
const closeBtn = document.getElementById('close-menu');
const menu     = document.getElementById('menu-burger');
const overlay  = document.getElementById('menu-overlay');

function openMenu() {
    menu.classList.add('active');
    overlay.classList.add('active');
}

function closeMenu() {
    menu.classList.remove('active');
    overlay.classList.remove('active');
}

if (openBtn)  openBtn.addEventListener('click', openMenu);
if (closeBtn) closeBtn.addEventListener('click', closeMenu);
if (overlay)  overlay.addEventListener('click', closeMenu);
</script>
</body>
</html>