<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion des entreprises - AlloStage</title>

    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/gestion-entreprises.css">
</head>

<body>

<!-- ================= MENU BURGER ================= -->
<div id="menu-overlay" class="menu-overlay"></div>

<div id="menu-burger" class="menu-burger">

    <button id="close-menu" class="close-btn">✕</button>

    <div class="menu-content">

        <a href="index.php">🏠 Accueil</a>
        <a href="index.php?page=offres">💼 Offres de stages</a>
        <a href="index.php?page=entreprises">🏢 Entreprises</a>

        {% if session.user is defined %}

            {% if session.user.role == 'etudiant' %}
                <a href="index.php?page=wishlist">❤️ Ma Wish-list</a>
                <a href="index.php?page=mes-candidatures">📄 Mes candidatures</a>

            {% elseif session.user.role == 'recruteur' %}
                <a href="index.php?page=gestion-etudiants">🎓 Gestion des étudiants</a>
                <a href="index.php?page=gestion-entreprises">🏢 Gestion des entreprises</a>
                <a href="index.php?page=gestion-offres">💼 Gestion des offres</a>
            {% elseif session.user.role == 'admin' %}
                <a href="index.php?page=gestion-pilotes">👨‍✈️ Gestion des pilotes</a>
                <a href="index.php?page=gestion-etudiants">🎓 Gestion des étudiants</a>
                <a href="index.php?page=gestion-entreprises">🏢 Gestion des entreprises</a>
                <a href="index.php?page=gestion-offres">💼 Gestion des offres</a>
            {% endif %}

        {% endif %}

    </div>

    <a href="index.php?page=logout" class="btn-logout">
        🔴 Déconnexion
    </a>

</div>

<!-- ================= HEADER ================= -->
<header class="entete">
    <div class="logo">
        <a href="index.php">
            <img src="/images/allostage.png" alt="Accueil AlloStage" class="logo-img">
        </a>
    </div>

    <nav class="navigation">
        <ul>
            <li><a href="index.php">Accueil</a></li>
            <li><a href="index.php?page=offres">Offres de stages</a></li>
            <li><a href="index.php?page=entreprises">Entreprises</a></li>
        </ul>
    </nav>

    <div class="actions-entete">
        {% if session.user is defined %}
            <button id="open-menu" class="btn-principal">
                Espace {{ session.user.role_label ?? session.user.role|capitalize }} ▾
            </button>
        {% else %}
            <a href="index.php?page=connexion" class="btn-connexion">
                Se connecter
            </a>
        {% endif %}
    </div>
</header>

<!-- ================= CONTENU ================= -->
<main class="contenu fond-gris">

    <div class="boite-blanche">

        <h1>Gestion des entreprises</h1>

        <div class="barre-recherche">
            <input type="text" placeholder="Rechercher une entreprise...">
            <button class="btn-principal">Rechercher</button>
        </div>

        <div class="ajout">
            <a href="index.php?page=entreprise-creer" class="btn-principal">
                + Ajouter une entreprise
            </a>
        </div>

        <table class="table-etudiants">
            <thead>
                <tr>
                    <th>Nom</th>
                    <th>Domaine</th>
                    <th>Ville</th>
                    <th>Email</th>
                    <th>Actions</th>
                </tr>
            </thead>

            <tbody>
                {% for entreprise in entreprises %}
                    <tr>
                        <td>{{ entreprise.nom_entreprise }}</td>
                        <td>{{ entreprise.domaine }}</td>
                        <td>{{ entreprise.ville ?? '-' }}</td>
                        <td>{{ entreprise.email }}</td>

                        <td class="actions">
                            <a href="index.php?page=entreprise-modifier&id={{ entreprise.id_entreprise }}"
                               class="btn-modifier">
                                Modifier
                            </a>

                            <form action="index.php?page=entreprise-supprimer&id={{ entreprise.id_entreprise }}" method="POST">
                                <button type="submit"
                                        class="btn-supprimer"
                                        onclick="return confirm('Supprimer cette entreprise ?')">
                                    Supprimer
                                </button>
                            </form>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5">Aucune entreprise trouvée</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

    </div>

</main>

<!-- ================= FOOTER ================= -->
<footer class="pied-de-page">
    <p>&copy; 2026 AlloStage</p>
</footer>

<!-- ================= SCRIPT ================= -->
<script>
const openBtn  = document.getElementById('open-menu');
const closeBtn = document.getElementById('close-menu');
const menu     = document.getElementById('menu-burger');
const overlay  = document.getElementById('menu-overlay');

function openMenu() {
    menu.classList.add('active');
    overlay.classList.add('active');
}

function closeMenu() {
    menu.classList.remove('active');
    overlay.classList.remove('active');
}

if (openBtn) openBtn.addEventListener('click', openMenu);
if (closeBtn) closeBtn.addEventListener('click', closeMenu);
if (overlay) overlay.addEventListener('click', closeMenu);
</script>

</body>
</html>